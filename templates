<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <title>Stock Screener | EWO Live</title>
  <script src="https://cdn.tailwindcss.com"></script>
  <meta http-equiv="refresh" content="60">
</head>
<body class="bg-[#0f172a] text-gray-100 font-sans">
  <div class="text-center py-6">
    <h1 class="text-3xl font-bold bg-gradient-to-r from-green-400 to-blue-500 bg-clip-text text-transparent">
      🔥 Bullish–Bearish Screener
    </h1>
    <p class="text-sm text-gray-400 mt-1">Updated: {{ data['last_update'] }}</p>
  </div>

  <div class="grid grid-cols-2 gap-6 px-8">
    <!-- Bullish -->
    <div>
      <h2 class="text-2xl text-green-400 font-semibold mb-3">Bullish Stocks</h2>
      {% for s in data['bullish'] %}
        <div class="bg-green-900/20 border border-green-600 rounded-xl p-4 mb-2 shadow-md">
          <p class="text-lg font-bold">{{ s.symbol }}</p>
          <p class="text-sm text-gray-300">Sector: {{ s.sector }}</p>
          <p class="text-sm">EWO(1D): {{ s.ewo_1d }} | EWO(5m): {{ s.ewo_5m }}</p>
        </div>
      {% endfor %}
    </div>

    <!-- Bearish -->
    <div>
      <h2 class="text-2xl text-red-400 font-semibold mb-3">Bearish Stocks</h2>
      {% for s in data['bearish'] %}
        <div class="bg-red-900/20 border border-red-600 rounded-xl p-4 mb-2 shadow-md">
          <p class="text-lg font-bold">{{ s.symbol }}</p>
          <p class="text-sm text-gray-300">Sector: {{ s.sector }}</p>
          <p class="text-sm">EWO(1D): {{ s.ewo_1d }} | EWO(5m): {{ s.ewo_5m }}</p>
        </div>
      {% endfor %}
    </div>
  </div>
</body>
</html>
